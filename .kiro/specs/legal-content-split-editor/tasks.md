# 实现计划：法律法规内容编辑和自动拆分

## 概述

本实现计划将"法律法规内容编辑和自动拆分"功能分解为可执行的开发任务。任务按照依赖关系组织，每个任务都包含明确的实现目标和验收标准。

## 任务列表

- [x] 1. 安装依赖和准备工作
  - 使用 bun 安装 markstream-vue 包
  - 验证 RichTextEditor 组件可用性
  - 验证 ResizablePanelGroup 组件可用性
  - _需求：3.1_

- [x] 2. 实现法律内容解析服务
  - [x] 2.1 创建解析服务文件和类型定义
    - 创建 `server/services/legal/parser.service.ts`
    - 定义 `ParsedArticle` 类型
    - 移植 `convertChineseNumberToArabic` 函数
    - _需求：8.7_

  - [ ]* 2.2 编写中文数字转换属性测试
    - **属性 3：中文数字转换正确性**
    - **验证：需求 8.7**

  - [x] 2.3 移植系统一解析器（Markdown 格式）
    - 移植 `parseDocument` 函数
    - 移植 `parseHeadingsAndContent` 函数
    - 移植 `parseMarkdownToNumber` 函数
    - 添加错误处理和日志记录
    - _需求：8.1_

  - [ ]* 2.4 编写系统一解析器属性测试
    - **属性 1：内容解析一致性（Markdown 部分）**
    - **验证：需求 4.2, 8.1**

  - [x] 2.5 移植系统二解析器（司法文档格式）
    - 移植 `parseJudicialDocument` 函数
    - 移植 `parseJudicialCoreContent` 函数
    - 添加错误处理和日志记录
    - _需求：8.2_

  - [ ]* 2.6 编写系统二解析器属性测试
    - **属性 1：内容解析一致性（司法文档部分）**
    - **验证：需求 4.3, 8.2**

  - [x] 2.7 移植共享辅助函数
    - 移植 `parseBottomContent` 函数
    - 移植 `createBottomContentItem` 函数
    - 移植 `parseAnnexes` 函数
    - _需求：8.3, 8.4, 8.5, 8.6_

  - [ ]* 2.8 编写标签解析属性测试
    - **属性 2：标签解析正确性**
    - **验证：需求 8.3, 8.4, 8.5, 8.6**

  - [x] 2.9 实现自动解析器选择函数
    - 实现 `parseContent` 函数，自动选择系统一或系统二
    - 添加解析错误捕获和友好错误消息
    - _需求：4.2, 4.3, 11.1_

  - [ ]* 2.10 编写解析错误处理属性测试
    - **属性 12：解析错误处理**
    - **验证：需求 4.4, 11.1**

- [x] 3. 实现条文批量保存服务
  - [x] 3.1 创建条文服务文件
    - 创建 `server/services/legal/article.service.ts`
    - 定义服务接口类型
    - _需求：6.2, 6.3_

  - [x] 3.2 实现更新法律内容函数
    - 实现 `updateLegalContent` 函数
    - 更新 legal_main 表的 content 字段
    - _需求：6.3_

  - [ ]* 3.3 编写法律内容同步更新属性测试
    - **属性 17：法律内容同步更新**
    - **验证：需求 6.3**

  - [x] 3.4 实现删除条文函数
    - 实现 `deleteArticlesByLegalId` 函数
    - 使用软删除（设置 deletedAt）
    - _需求：6.2_

  - [x] 3.5 实现批量创建条文函数
    - 实现 `createArticles` 函数
    - 生成 UUID v7 标识符
    - 设置 order 字段
    - 继承法律法规的日期字段
    - _需求：6.3, 9.3, 9.4, 9.5_

  - [ ]* 3.6 编写条文创建属性测试
    - **属性 5：条文 ID 唯一性**
    - **属性 6：条文顺序正确性**
    - **属性 7：日期继承正确性**
    - **验证：需求 9.3, 9.4, 9.5**

  - [x] 3.7 实现批量保存条文函数
    - 实现 `batchSaveArticles` 函数
    - 使用数据库事务
    - 调用 updateLegalContent、deleteArticlesByLegalId、createArticles
    - 添加事务回滚处理
    - _需求：6.2, 6.3, 6.5, 9.1, 9.2_

  - [ ]* 3.8 编写条文保存原子性属性测试
    - **属性 4：条文保存原子性**
    - **验证：需求 6.2, 6.3, 6.5, 9.1, 9.2**

- [x] 4. 实现批量保存条文 API
  - [x] 4.1 创建 API 端点文件
    - 创建 `server/api/v1/admin/legal-articles/batch-save.post.ts`
    - 定义请求和响应类型
    - _需求：6.1, 6.2, 6.3_

  - [x] 4.2 实现请求参数验证
    - 使用 zod 验证 legalId、content、articles
    - 验证内容不为空
    - _需求：6.1_

  - [ ]* 4.3 编写空内容验证属性测试
    - **属性 11：空内容验证**
    - **验证：需求 6.1**

  - [x] 4.4 实现保存处理逻辑
    - 验证用户权限
    - 获取法律法规信息（日期字段）
    - 调用 batchSaveArticles 服务
    - 处理错误和事务回滚
    - _需求：6.2, 6.3, 6.5, 11.2_

  - [x] 4.5 实现向量化触发
    - 保存成功后调用批量向量化 API
    - 向量化失败不影响保存结果
    - 记录向量化错误日志
    - _需求：6.4, 7.1, 7.4, 11.3_

  - [ ]* 4.6 编写向量化触发属性测试
    - **属性 8：向量化触发正确性**
    - **验证：需求 6.4, 7.1, 7.4, 11.3**

- [x] 5. 实现前端 Composable
  - [x] 5.1 创建法律内容解析 Composable
    - 创建 `app/composables/useLegalParser.ts`
    - 实现 `parse` 函数（调用前端解析逻辑或 API）
    - 提供 `parseError` 响应式状态
    - _需求：4.1, 4.2, 4.3, 4.4_

  - [x] 5.2 创建本地缓存 Composable
    - 创建 `app/composables/useLegalEditorCache.ts`
    - 实现 `saveDraftToCache` 函数
    - 实现 `loadDraftFromCache` 函数
    - 实现 `clearDraftCache` 函数
    - 使用防抖处理自动保存
    - _需求：2.2_

  - [ ]* 5.3 编写草稿缓存属性测试
    - **属性 15：草稿缓存正确性**
    - **属性 16：草稿加载优先级**
    - **验证：需求 2.2**

- [x] 6. 实现条文预览组件
  - [x] 6.1 创建条文预览组件
    - 创建 `app/components/legal/LegalArticlePreview.vue`
    - 使用 markstream-vue 渲染 Markdown 内容
    - 按层级缩进展示条文结构
    - 显示解析错误信息
    - _需求：3.2, 3.4, 4.4, 4.5_

  - [ ]* 6.2 编写预览组件单元测试
    - 测试 markstream-vue 组件渲染
    - 测试层级缩进显示
    - 测试错误信息显示

- [x] 7. 实现全量更新编辑页面
  - [x] 7.1 创建页面文件和路由
    - 创建 `app/pages/admin/legal-main/[id]/full-update.vue`
    - 配置页面元数据和布局
    - _需求：1.1_

  - [x] 7.2 实现页面状态管理
    - 定义 EditorState 接口
    - 实现响应式状态（content, parsedArticles, parseError, saving, hasUnsavedChanges）
    - _需求：2.2, 4.1_

  - [x] 7.3 实现内容加载逻辑
    - 实现 `loadLegalContent` 函数
    - 优先从缓存加载草稿
    - 回退到数据库加载
    - _需求：2.2_

  - [x] 7.4 实现桌面端布局
    - 使用 ResizablePanelGroup 实现左右分栏
    - 左侧：RichTextEditor（outputFormat="markdown"）
    - 右侧：LegalArticlePreview
    - 添加 ResizableHandle
    - _需求：1.2, 2.1, 3.2, 5.1_

  - [x] 7.5 实现移动端布局
    - 检测窗口宽度（<768px）
    - 实现编辑/预览模式切换
    - 添加切换按钮
    - _需求：1.3, 5.2_

  - [ ]* 7.6 编写响应式布局属性测试
    - **属性 10：响应式布局正确性**
    - **验证：需求 1.2, 5.1, 5.2**

  - [x] 7.7 实现内容变化处理
    - 实现 `handleContentChange` 函数
    - 触发实时解析
    - 自动保存草稿到缓存（防抖）
    - 更新 hasUnsavedChanges 状态
    - _需求：2.2, 4.1_

  - [ ]* 7.8 编写实时预览同步属性测试
    - **属性 9：实时预览同步性**
    - **验证：需求 3.4, 4.1**

  - [x] 7.9 实现保存处理
    - 实现 `handleSave` 函数
    - 验证内容不为空
    - 调用批量保存 API
    - 显示加载状态
    - 保存成功后清除缓存
    - 显示成功提示并跳转
    - _需求：6.1, 6.2, 6.3, 6.4, 10.1, 10.2_

  - [x] 7.10 实现取消处理
    - 实现 `handleCancel` 函数
    - 检查未保存更改
    - 显示确认对话框
    - 返回列表页
    - _需求：10.4_

  - [ ]* 7.11 编写未保存更改提示属性测试
    - **属性 13：未保存更改提示**
    - **验证：需求 10.4**

  - [x] 7.12 实现错误处理
    - 捕获解析错误并显示
    - 捕获保存错误并显示
    - 捕获网络超时并提供重试
    - _需求：10.3, 11.1, 11.2, 11.4_

  - [x] 7.13 实现键盘快捷键
    - 监听 Ctrl+S / Cmd+S
    - 触发保存操作
    - _需求：10.1_

- [x] 8. 添加全量更新入口
  - [x] 8.1 在法律法规编辑页面添加按钮
    - 在 `app/pages/admin/legal-main/[id]/index.vue` 添加"全量更新"按钮
    - 点击跳转到 full-update 页面
    - _需求：1.1_

  - [ ]* 8.2 编写全量更新入口单元测试
    - 测试按钮点击跳转

- [x] 9. 实现退出登录时清除缓存
  - [x] 9.1 在登出逻辑中添加缓存清除
    - 查找登出处理函数
    - 调用清除所有编辑器缓存的函数
    - _需求：2.2_

- [ ] 10. 集成测试和 UI 测试
  - [ ]* 10.1 编写完整流程集成测试
    - 测试从编辑到保存到向量化的完整流程
    - 测试事务回滚
    - 测试并发保存

  - [ ]* 10.2 编写 UI 自动化测试（vibium）
    - 测试编辑器和预览器实时同步
    - 测试响应式布局切换
    - 测试保存和取消操作
    - 测试错误提示显示

- [ ] 11. 文档和优化
  - [ ]* 11.1 编写 API 文档
    - 记录批量保存 API 的使用方法
    - 提供请求和响应示例

  - [ ]* 11.2 性能优化
    - 优化实时解析的防抖时间
    - 考虑大量条文的虚拟滚动
    - 优化数据库批量插入性能

  - [ ]* 11.3 用户体验优化
    - 添加保存进度提示
    - 优化错误提示文案
    - 添加操作引导提示

## 注意事项

- 标记 `*` 的任务为可选任务，可以根据时间和优先级决定是否实施
- 每个任务完成后应运行相关测试确保功能正常
- 属性测试应配置运行 100 次迭代
- 集成测试需要真实的数据库操作，不使用 mock
- UI 测试使用测试账号：13064768490 / daixin88

## 检查点

- [x] 检查点 1：解析服务实现完成
  - 确保所有解析器测试通过
  - 确保中文数字转换正确
  - 确保错误处理正常

- [x] 检查点 2：后端 API 实现完成
  - 确保批量保存 API 测试通过
  - 确保事务回滚正常
  - 确保向量化触发正常

- [x] 检查点 3：前端页面实现完成
  - 确保编辑器和预览器正常工作
  - 确保响应式布局正常
  - 确保本地缓存正常

- [ ] 检查点 4：集成测试通过
  - 确保完整流程测试通过
  - 确保 UI 测试通过
  - 确保所有属性测试通过
