/**
 * 加班倍率常量（劳动法法定标准）
 *
 * 来源：
 * - 《劳动法》第四十四条
 * - 工作日加班：不低于工资的 150%
 * - 休息日加班（不补休）：不低于工资的 200%
 * - 法定节假日加班：不低于工资的 300%
 *
 * 调休倍率说明：
 * - 工作日/休息日加班可安排补休，补休比例为 1:1
 * - 法定节假日加班不可用补休替代，必须支付 300% 工资
 *   （此处 compensatory 仅用于计算实际需支付的倍率差）
 */

/** 加班费倍率（相对小时工资的乘数） */
export const OVERTIME_RATES = {
    /** 工作日加班：1.5 倍 */
    workday: 1.5,
    /** 休息日加班：2 倍 */
    weekend: 2,
    /** 法定节假日加班：3 倍 */
    holiday: 3,
} as const

/** 调休折算倍率（加班小时数对应可换算的调休小时数） */
export const COMPENSATORY_RATES = {
    /** 工作日加班调休：1:1 */
    workday: 1,
    /** 休息日加班调休：1:1 */
    weekend: 1,
    /** 法定节假日加班调休：1:3（需补足工资差） */
    holiday: 3,
} as const
